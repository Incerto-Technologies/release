{{- if .Values.persistence.enabled }}
# Backend Logs PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-backend-logs
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.backend.logs.size }}

---
# Backend Resource Scripts PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-backend-scripts
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.backend.resourceScripts.size }}

---
# Backend Resource PEM PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-backend-pem
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.backend.resourcePem.size }}

---
# Backend Resource Source PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-backend-source
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.backend.resourceSource.size }}

---
# Backend Config RBAC PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-backend-rbac
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.backend.configRbac.size }}

---
# AI Logs PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "incerto-tool.fullname" . }}-ai-logs
  labels:
    {{- include "incerto-tool.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.storageClass }}
  storageClassName: {{ .Values.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.ai.logs.size }}
{{- end }}